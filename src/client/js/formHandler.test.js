import {
    handleSubmit
} from "./js/formHandler";

describe('handleSubmit', () => {
    test('handleSubmit function should be defined', () => {
        epect(handleSubmit).toBeDefined();
    });

});